# Cumulative Agony patch — baseline 3d78c873

Authoritative input: `Агония.docx`, all 11 rendered pages read in order;
comments follow the preceding image group, including page breaks. Original DOCX
and archives are not modified. The immutable 56-family fingerprint was captured
before production edits (`baseline-fingerprints.json.gz`).

| DOCX group | Proven current IDs | Scoped action |
|---|---|---|
| Landing, p1 | o_ladder_01 | Symmetric edge artwork, simple standable platform; separate item |
| Fence fragment, p2 | o_iron_railing | User-approved retirement; retain exact source evidence as unconsumed CONTEXT_ONLY, not an invented replacement |
| Candlesticks, p3 | o_candles_0 | Support each independently authored candlestick; preserve source XYZ |
| Cases, p4–5 | o_cases_0, o_c561, o_c046 | Exact C561→C046; keep C003 CASES unless exact textured evidence proves identity |
| Sacks, p6–7 | o_c1962_a, o_c1962_b, o_bag | One o_bag item, C1962_a provenance; preserve four model variants |
| Books, p8 | o_c1319, o_books | Keep o_books, C003 component 2; preserve weighted variants |
| Doors, p9 | Both o_acacia_door, open=false | Already one family/item. Two gallery specimens were both closed after interaction; no other door is deleted |
| Benches, p10 | o_bench, o_bench_rotate | o_bench survivor, facing + diagonal baked state, three authored seats |
| Trees, p11 | Both o_c001, tree_254d81d6bf3e, BASE/ALT | Already one family/item. Keep genuine variants and BASE/ALT; no identity deletion based on duplicate proof specimens |

Additional prompt scope: o_shuttered_window safe in-wall opening; attachments
for all four C008 statues; retire o_ladder_02 with preserved unconsumed context
rules; o_ladder_03 wall plane and stack-five; descriptive production labels.

Implementation order: targeted runtime/data changes → explicit successor/context
coverage → untouched fingerprint gate → narrow tests → accumulated check/build/
GameTests → fresh compact gallery and release. No global palette generator,
normalizer rewrite, source scan, city conversion or batch-03.

The existing global Java hashes are evidence, not proof of per-family behavior:
runtime changes receive independent review and server tests. The gate compares
all unaffected family data, raw textured meshes, transforms, source patterns,
shapes, properties and resource hashes. Display-name changes have a separate
allowlist and never waive geometry checks.
